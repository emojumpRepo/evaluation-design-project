# 排除node_modules，在Docker内部重新安装
node_modules
*/node_modules
**/node_modules

# 排除构建产物
dist
build
*/dist
*/build

# 排除开发工具和配置文件
.git
.gitignore
README.md
README_EN.md
.env
.env.local
.env.*.local

# 排除IDE和编辑器文件
.vscode
.idea
*.swp
*.swo
*~

# 排除日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 排除临时文件
.DS_Store
Thumbs.db

# 排除Docker相关文件
Dockerfile
docker-compose.yaml
docker-compose.yml
.dockerignore

# 排除包管理器锁文件（让Docker内部重新生成）
package-lock.json
yarn.lock
pnpm-lock.yaml

# 排除用户上传和导出文件
userUpload
exportfile

# 排除测试覆盖率报告
coverage
*.lcov

# 排除其他不必要的文件
.history
.expo
lib
.yarn 